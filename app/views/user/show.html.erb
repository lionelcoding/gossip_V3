<nav class="navbar navbar-expand-lg navbar-light bg-light">
  
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link active" href="/">Home 
      <a class="nav-item nav-link" href="team">team</a>
      <a class="nav-item nav-link" href="contact">contact</a>
      <%# <% if current_users %>  
      <% if logged_in? %>
         <a class="nav-link nav-item" href="<%= new_user_path %>">sign_in</a>
      
         <a class="nav-link nav-item" href="<%= new_session_path %>">session</a>
      
      <% else %>
         
         <a class="nav-link nav-item"><%=  link_to "Log out",  logout_path, method: "delete" %> </a> 
        
      <% end %>     
    </div>
  </div>
</nav>
 


<div class="page content">
  <p style="font-size: 22px"><u><b>Profile of <%= @user.first_name%></b></u></p><br>
  <p><b> First Name: </b><%= @user.first_name%></p>
  <p><b> Last Name: </b><%= @user.last_name%></p>
  <p><b> Email: </b><%= @user.email%></p>
  <p><b> Age: </b><%= @user.age%></p>
  <p><b> City: </b>
  <p><b> Description: </b><%= @user.description%></p>
  <p class="datetime"><em><%= @user.updated_at %><em></p>
</div>